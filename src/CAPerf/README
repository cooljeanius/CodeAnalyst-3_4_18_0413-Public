Example of how to configure and build libCAData on Linux

./autogen.sh 
./configure --with-CommonProjectsHdr=$COMMON_DIR/Src/CommonProjectsHdr --with-libcadata=$COMMON_DIR/Lib/AMD/libCaData
make clean 
make -j4 all
make install

The library will be installed in /opt/CAPerf
